<Window x:Class="HanoriMvvm.UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:HanoriMvvm.UI.Views"
        Style="{StaticResource MainWindow}">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="55"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding SystemBar}"/>
        <ContentControl Grid.Row="1" Content="{Binding }"/>
        <ContentControl Grid.Row="2" Content="{Binding }"/>
        <ContentControl Grid.Row="3" Content="{Binding }"/>
    </Grid>
</Window>
